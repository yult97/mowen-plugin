websockets>=12.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
structlog>=24.0
pytest>=8.0
pytest-asyncio>=0.23
python-socks[asyncio]>=2.4.0
httpx>=0.27.0
volcengine-python-sdk[ark]>=1.0.0

# 可选依赖：语义分段 BERT 模型（如果启用 SEMANTIC_ENABLE_BERT=true）
# 取消注释以下两行来安装：
# transformers>=4.30.0
# torch>=2.0.0
