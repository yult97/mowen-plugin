import React from 'react';
import ReactDOM from 'react-dom/client';
import NotesExportPage from './NotesExportPage';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NotesExportPage />
  </React.StrictMode>
);
